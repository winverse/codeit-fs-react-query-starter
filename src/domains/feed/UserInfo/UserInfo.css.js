import { style } from '@vanilla-extract/css';

export const userInfo = style({
  display: 'flex',
  alignItems: 'center',
  marginBottom: '10px',
});

export const userName = style({
  fontSize: '14px',
  fontWeight: 500,
  marginLeft: '10px',
  color: '#494949',
});

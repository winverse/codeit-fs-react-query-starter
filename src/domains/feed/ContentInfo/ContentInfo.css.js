import { style } from '@vanilla-extract/css';

export const info = style({
  display: 'flex',
});

export const date = style({
  marginLeft: 'auto',
  fontSize: '12px',
  fontWeight: 300,
  color: 'grey',
});
